# 2FA CE（2FA-Chrome Extension）隐私政策

**最后更新：** 2026年8月24日

## 概述

2FA CE（以下简称"本扩展"）是一款浏览器扩展，用于在浏览器本地生成 TOTP（基于时间的一次性密码）验证码。本隐私政策说明本扩展收集哪些数据、如何使用这些数据，以及你的权利。

## 我们收集的数据

**我们不收集任何个人数据。** 本扩展不会向任何外部服务器传输信息。所有数据仅存储在你的本地设备上。

### 本地存储的数据

本扩展在浏览器本地存储（`chrome.storage.local`）中保存以下数据：

- **2FA 密钥：** 从你扫描的二维码中提取的共享密钥（TOTP 密钥），仅用于在本地生成验证码。
- **域名映射：** 每个密钥关联的域名或 URL，用于自动识别在哪个网站显示哪个验证码。
- **用户偏好：** 如是否在2FA页面自动提示等设置。

这些数据永远不会离开你的设备，不会同步到任何云端服务，不会发送到任何服务器，开发者也无法访问。

## 我们不收集的数据

- 不收集你的浏览历史
- 不收集你的 IP 地址或位置信息
- 不收集个人身份信息（姓名、邮箱等）
- 不使用分析或追踪工具
- 不显示广告
- 不与任何第三方共享数据

## 我们如何使用你的数据

- **生成 TOTP 验证码：** 存储的密钥通过 Web Crypto API 在本地生成基于时间的验证码。
- **页面检测：** 本扩展检查当前页面的 URL 和 DOM 以识别2FA登录页面和二维码图片，此过程完全在浏览器中进行。
- **无远程处理：** 没有任何数据被发送到服务器进行处理。

## 权限说明

| 权限 | 用途 |
|---|---|
| `storage` | 在本地存储2FA密钥和用户设置 |
| `activeTab` | 访问当前活动标签页以检测2FA页面和扫描二维码 |
| `scripting` | 注入内容脚本以分析页面内容中的2FA元素 |
| `notifications` | 检测到2FA页面或复制验证码时显示浏览器通知 |
| `<all_urls>`（host_permissions） | 支持在你访问的任意网站上进行2FA检测和二维码扫描。本扩展不使用此权限收集浏览数据 |

## 远程代码声明

本扩展**不**使用或执行任何远程代码。所有 JavaScript 均打包在扩展包内并在本地运行。唯一包含的外部库是 jsQR（纯 JavaScript 二维码解析库），已本地打包，不发起网络请求。

## 数据保留与删除

- 所有数据保留在你的设备上，直到你手动删除。
- 你可以随时通过扩展的选项页面删除单个密钥或全部数据。
- 卸载扩展将删除所有本地存储的数据。

## 安全

- TOTP 验证码使用 Web Crypto API（`crypto.subtle`）生成，提供安全的加密操作。
- 密钥存储在 `chrome.storage.local` 中，按浏览器配置文件隔离，其他扩展或网站无法访问。
- 本扩展不使用 `eval()` 或其他动态代码执行。

## 本政策的变更

我们可能会不时更新本隐私政策。任何变更将反映在扩展的商店列表和本文档中。

## 联系我们

如对本隐私政策或扩展有任何疑问，请在 GitHub 仓库提交 Issue：
https://github.com/Trinity-Plugins/2FA-Chrome-Extension
